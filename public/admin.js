let token=localStorage.getItem('bun_admin_token');const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const api=async(path,opt={})=>{opt.headers={...(opt.headers||{}),'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})};const r=await fetch(path,opt);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d};function show(){$('#loginCard').classList.add('hidden');$('#panel').classList.remove('hidden');loadProducts()}function hide(){$('#loginCard').classList.remove('hidden');$('#panel').classList.add('hidden')}$('#loginForm').onsubmit=async e=>{e.preventDefault();try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:$('#username').value,password:$('#password').value})});token=d.token;localStorage.setItem('bun_admin_token',token);show()}catch(x){$('#loginMsg').textContent=x.message}};$('#logout').onclick=()=>{localStorage.removeItem('bun_admin_token');token=null;hide()};$('#productForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/products',{method:'POST',body:JSON.stringify({name:$('#name').value,price:Number($('#price').value),category:$('#category').value,icon:$('#icon').value,image:$('#image').value,description:$('#description').value,stock:Number($('#stock').value)})});$('#productMsg').textContent='Product published to the store.';e.target.reset();$('#icon').value='🛍️';$('#stock').value=1;loadProducts()}catch(x){$('#productMsg').textContent=x.message}};async function loadProducts(){try{const d=await api('/api/products/admin');$('#productList').innerHTML=d.map(p=>`<div class="admin-product"><div>${p.image?`<img src="${esc(p.image)}" alt="">`:`<span class="admin-icon">${esc(p.icon)}</span>`}</div><div class="admin-product-info"><b>${esc(p.name)}</b><span>KSh ${Number(p.price).toLocaleString()} · ${esc(p.category)} · Stock: ${p.stock}</span></div><button onclick="removeProduct(${p.id})">Delete</button></div>`).join('')||'<p class="muted">No products yet.</p>'}catch(x){if(x.message.includes('Authentication')){localStorage.removeItem('bun_admin_token');token=null;hide()}}}async function removeProduct(id){if(!confirm('Delete this product?'))return;try{await api('/api/products/'+id,{method:'DELETE'});loadProducts()}catch(x){alert(x.message)}}if(token)show();else hide();